steps:
  # Install Go (if needed)
  - name: 'golang:1.20'
    id: 'setup-go'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        apt-get update && apt-get install -y golang-go
        go version
        go mod download
